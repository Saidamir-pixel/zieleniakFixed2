<?php
// Путь к файлу настроек
// $settingsFile = __DIR__ . '/settings.json';

// Получаем данные из POST-запроса
// if (isset($_POST['orders_enabled'])) {
//     $ordersEnabled = $_POST['orders_enabled'] == 'true' ? true : false;

    // Читаем текущие настройки
    // if (file_exists($settingsFile)) {
    //     $settings = json_decode(file_get_contents($settingsFile), true);
    // } else {
    //     $settings = [];
    // }

    // Обновляем состояние
    // $settings['orders_enabled'] = $ordersEnabled;

    // Сохраняем изменения в файл
//     if (file_put_contents($settingsFile, json_encode($settings))) {
//         echo json_encode(['status' => 'success', 'message' => 'Состояние заказов обновлено.']);
//     } else {
//         echo json_encode(['status' => 'error', 'message' => 'Ошибка при сохранении состояния.']);
//     }
// } else {
//     echo json_encode(['status' => 'error', 'message' => 'Нет данных для обновления.']);
// }
?>
